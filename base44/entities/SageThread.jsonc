{
  "name": "SageThread",
  "type": "object",
  "description": "One Sage conversation. Created by the sage-ask function on the first question, titled from that question, and renameable by its owner. Strictly private: only the person it belongs to can ever read it.",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "Owner. Only they may read or write this row."
    },
    "title": {
      "type": "string",
      "maxLength": 120,
      "description": "Shown in the conversation list. Auto-written from the first question, then editable."
    },
    "folder_id": {
      "type": "string",
      "description": "The SageFolder this chat is filed under, empty when unfiled. Set by the owner from the chat list."
    },
    "archived": {
      "type": "boolean",
      "default": false,
      "description": "Hidden from the main chat list but kept, so a finished project can be tucked away without deleting it. Set by the owner and reversible."
    },
    "last_message_at": {
      "type": "string",
      "description": "ISO timestamp of the most recent message, used to order the list"
    },
    "message_count": {
      "type": "number",
      "default": 0
    }
  },
  "required": [
    "user_email"
  ],
  "rls": {
    "create": {
      "data.user_email": "{{user.email}}"
    },
    "read": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "data.user_email": "{{user.email}}"
    },
    "delete": {
      "$or": [
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}